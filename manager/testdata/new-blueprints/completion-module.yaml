apiVersion: app.m4d.ibm.com/v1alpha2
kind: M4DModule
metadata:
  name: completion
  namespace: m4d-system
  labels:
    version: 0.0.1
    name: completion-job
spec:
  chart:
    name: ghcr.io/mesh-for-data/m4d-copy-completion-job:0.1.0
  statusIndicators:
  capabilities:
  - capability: completion
    scope: dataset
    sinks:
    - protocol: mail
      dataformat: plaintext
    - protocol: slack
      dataformat: plaintext
    - protocol: kafka
      dataformat: json


